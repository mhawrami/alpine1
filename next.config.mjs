/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['de', 'en'], // Unterstützte Sprachen
    defaultLocale: 'de',  // Standardsprache
  },
};

export default nextConfig;
